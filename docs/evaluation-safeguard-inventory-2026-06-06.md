# Evaluation Safeguard Inventory — 2026-06-06

| Safeguard | Current status | Why it changed | Risk introduced | Restoration decision | Blocks user-facing readiness? |
| --- | --- | --- | --- | --- | --- |
| Legacy Perplexity post-synthesis Pass 4 | Retired | Replaced by dual-model/chunk-level scoring to reduce latency and timeout risk. | Removed a final independent report completeness check. | Restore only as compact `final_external_audit_v1`; do not send the full manuscript or let provider write prose. | Long-form: yes for missing artifacts/coverage/contradictions when blocking. Short-form: no by default. |
| Pass 3B / DREAM in main pipeline | Async | Moved to `/api/workers/process-dream` to avoid long-form timeout. | Job could look complete before narrative synthesis exists. | Keep async, but user-facing status now waits for `longform_document_v1` and final audit before “full report ready.” | Yes for long-form report-ready messaging. |
| LLR / deterministic output rules | Warning-only in some paths | Reduced false hard-stops during prior hardening. | Unsupported certainty or internal process leakage can reach short reports. | Add deterministic short-form final sanity check for fake certainty, internal leakage, missing anchors, and unsupported global claims. | Yes when deterministic sanity check blocks. |
| WAVE / Phase 5 readiness | Ambiguous / downstream | Some WAVE artifacts are post-eval or Revise-facing rather than required scoring steps. | Multi-layer reports may imply deeper readiness before required addenda exist. | Treat `wave_revision_plan_v1` as required only for detected multi-layer final audit contexts; no short-form WAVE requirement. | Multi-layer: yes. Standard long-form: no unless configured. |
| Short-form sparse routing | Active | Correctly avoids long-form cost and artifacts. | Sparse text can be over-scored if criteria lack direct anchors. | Add `short_form_evidence_gate_v1` metadata and deterministic confidence/scorability caps. | Yes through criterion status and sanity blocks; does not require long-form gates. |
| Short-form Perplexity | Constrained | Cost/latency should not increase for excerpts. | External provider could be invoked accidentally by long-form final audit path. | `SHORT_FORM_EXTERNAL_QA_MODE` defaults to `off`; normal short-form uses zero Perplexity calls. | No by default; dispute/premium/operator modes only. |
