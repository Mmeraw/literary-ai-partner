Stress Test Evaluation Process Yeah — I just caught it in the DB too. Froggin Noggin failed at Phase 1 at 08:30:09 UTC, ~768s in. And the error string you just pasted nails the actual failure mode, which is  not  plumbing: What just failed Error:  [QualityGateV2] v2_summary_weakness_presence: Overview summary omits bottom-score weakness criteria: pacing, proseControl , tone, narrativeClosure , marketability Decoding it: Pass 1 (or Pass 1 → roll-up) generated an overview summary The QualityGateV2 governance check v2_summary_weakness_presence enforces that the overview must explicitly name the criteria with the lowest scores The model omitted 5 of them → fail-closed This is  governance working correctly  — gpt-5.1 produced a soft, omission-prone summary, and the gate caught it This is the same family of failure as the PASS4_WEAK_AGREEMENT from your May 14 doc:  gates fire, model output isn't perfectly disciplined yet . Not a regression from PR-I/PR-J. Now — do those docs help with the stress test? Yes, massively.  They give us the full mental model that's been missing. Here's the synthesis: From STRESS-TEST-BUILD-PLAN-EVALUATION-PROCESS.docx — three-tier harness already specified Tier What Cost Cadence T1  Plumbing 22 rows, mock LLM + mock Supabase , control flow only $0 Every PR T2  Quality ~10 rows, live OpenAI, QG_* gates fire on real output $8–15/run Nightly / eval-touch PRs T3a  UI seeded Playwright on seeded DB rows $0 Every PR T3b  UI E2E Full upload → report flow, real pipeline + real UI $5–10/run Weekly From COMPUTER-STRESS-TEST-14-May-2026.docx — the real Pass 4 fix Multi-chunk, multi-phase evidence packet: ≥8 chunks for novel-length, early/mid/late spread, multiple anchors per criterion, compression ratio floor ≥ 0.03–0.05. That's the long-form Pass 4 evidence redesign. What's already in your repo vs. what's missing ✅   T1 mock harness lives  on main ( npm run pipeline:stress , 22 fixtures) 🟡   T2 live harness  = PR #485 — never merged; had 5 bugs from ChatGPT critique ( passTimeoutMs =90s, pass4_ms measuring wall time, coverage_min_pct unasserted, Supabase secrets unused, pull_request trigger) 🟡   PR #482  adds 4 Pass 4 stress fixtures to T1 (fault-injecting Perplexity mock) — still open, behind 16 ❌   T3a/T3b UI harness  — not built ❌   "100 fictitious evals"  — never built. That's a  fifth thing , distinct from T1/T2/T3. So what  is  "100 fictitious evals"? Based on the docs, your original meaning is a  batch-prod canary  — neither T1 (mock) nor T2 (10 live ground-truth rows) nor T3 (UI). It's the missing layer: Submit ~100 small synthetic manuscripts to  prod  via / api /jobs, monitor outcomes in  evaluation_jobs , surface failure-mode distribution. This is what catches the v2_summary_weakness_presence class of bug — gates that fire 1-in-N times on real LLM variance —  before  a real user submits Froggin Noggin and hits it. What I recommend, in order Right now — diagnose the v2_summary_weakness_presence fail  (next 15 min, while we plan). Pull the overview summary text from  evaluation_result  for a8d47d73 and confirm it's a prompt-discipline gap, not a logic bug in the gate. Decide stress-test scope tonight  — pick one of: (A) Batch-prod 100 synthetics  (the thing you asked about). Generate 100 small (1–5k word) synthetic manuscripts of varied quality, submit to prod, harvest results. Catches gate-fire distribution. ~$100–200 in OpenAI/Perplexity. (B) Land T2 properly  — finish PR #485 with the 4 must-fixes from your May 14 critique, ship truthful Tier 2, run nightly on 10 ground-truth rows. The doc's actual plan. (C) Both, sequenced  — finish PR #485 first (it earns the regression lock), then run the 100-eval canary against staging using the same secrets. Fix the actual Froggin Noggin blocker  — patch the Pass 1 overview-summary prompt so gpt-5.1 always names bottom-score weakness criteria. That's the real next PR. Which way do you want to go? My honest read:  (C) sequenced , because batch-prod without T2 means we'd discover the same gate fires 100 times without the diagnostic plumbing to triage them. But if you want fast signal tonight,  (A) standalone  can ship in ~2 hours with a synthetic generator + a submission script + a results query.
