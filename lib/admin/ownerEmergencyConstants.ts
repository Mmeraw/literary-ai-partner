/**
 * Client/server-safe constants for the owner emergency cancellation control.
 * NO server-only imports here. Safe to import from "use client" components.
 */

/** The exact phrase a user must type to confirm emergency cancellation. */
export const OWNER_EMERGENCY_CANCEL_CONFIRMATION = 'CANCEL ALL ACTIVE EVALUATIONS';
