# ROADMAP.md — ARCHIVED

**This file is no longer authoritative.**

The single source of truth for the RevisionGrade roadmap is the workbook:

`docs/roadmap/RevisionGradeSystemLedgerCURRENT.xlsx`

Per system doctrine (`If it is not enforced, it is not real.`), all roadmap state — U-layer status, RCAs, execution block, enforcement scale, verification artifacts — lives in the workbook. Markdown and CSV mirrors are archived and must not be treated as parallel truth.

See also: `docs/roadmap/README.md` for the workbook authority pointer.
