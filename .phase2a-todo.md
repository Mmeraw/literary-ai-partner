# Phase 2A - Final Polish Todo

**Status:** ✅ COMPLETE  
**Source:** ChatGPT recommendations for production-grade scripts  
**Date Completed:** 2026-01-27

## Checklist

### Script Hardening
- [x] Replace `set -e` with `set -euo pipefail` (fail on unset vars, fail in pipes)
- [x] Add `tr -d '[:space:]'` to SQL output cleaning (removes whitespace)
- [x] Improve PID file validation (check if empty string)
- [x] Use `setsid bash -lc 'exec npx tsx ...'` for cleaner process tree
- [x] Better variable quoting patterns (`"${VAR:-}"`)
- [x] Add explicit DOTENV_CONFIG_PATH export in worker-start.sh
- [x] Validate PID before writing to file (confirm process exists)

### Migration Safety
- [x] Migration already safe (uses ::text cast, checks UUID type first)

### Verification
- [x] All scripts use `set -euo pipefail`
- [x] SQL output cleaned with `tr -d '[:space:]'`
- [x] Worker lifecycle tested (start/stop/verify)
- [x] Zero orphaned processes confirmed

## Production-Grade Features Now Active

✅ **Strict error handling**: Scripts fail on unset variables, pipe failures  
✅ **Defensive PID validation**: Empty file check, process existence check  
✅ **Clean SQL output**: `tr` removes whitespace from psql output  
✅ **Explicit exports**: DOTENV_CONFIG_PATH explicitly exported  
✅ **Process tree cleanup**: `setsid bash -lc 'exec ...'` for clean hierarchy  
✅ **Pre-write validation**: Confirm worker started before writing PID file  

**Status**: Phase 2A hardening locked in with production-grade polish.
