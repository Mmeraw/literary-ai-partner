name: Canon Guard

on:
  pull_request:
  push:
    branches: [main]

jobs:
  canon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Run Canon Guard
        run: bash ./scripts/canon-guard.sh
